pipeline {
    agent any

    stages {
        stage('Clone repo') {
            steps {
                sh 'git clone git@github.com:adnaan0l/aws-api-deployment.git .'
            }
        }
    }
}
